import(methods)
export(uuid,boost.mutex)
exportClasses(
  mutex,
  boost.mutex,
  descriptor,
  boost.mutex.descriptor)
exportMethods(
  lock, 
  lock.shared,
  unlock,
  is.timed,
  timeout,
  shared.name,
  describe,
  attach.resource)
useDynLib(synchronicity)

import(methods)
importFrom(utils, head, tail)

exportClasses(big.matrix, big.matrix.descriptor)

export(big.matrix,
       filebacked.big.matrix,
       attach.big.matrix,
       attach.resource,
       sub.big.matrix,
       deepcopy)

exportMethods("[", "[<-",
              dimnames, 
              nrow, ncol,
              print,
              head, tail,
              typeof,
              dim,
              is.big.matrix, as.big.matrix,
              describe,
              mwhich,
              read.big.matrix, write.big.matrix,
              is.separated,
              is.filebacked,
              is.sub.big.matrix,
              backingpath,
              apply)

useDynLib(bigmemory)

